<%title @region.name %>
<div class="learning-results clearfix"> 
<h1>
  REGION &gt; 
  <%= @region.name %>
</h1>

<%= render :partial => 'offerings/offerings_list' %>

<% if current_admin %>
  <%= link_to 'Edit', edit_region_path(@region) %> |
<% end %>
<%= link_to 'Back', regions_path %>
</div>
