<%title "Offerings by Topic" %>

<div class="learning-results clearfix">
  <h1>BROWSE &gt; by TOPIC</h1>
  <div class="clear"></div> 

  <% @topics.each do |topic| %>

    <div class="topic-gallery">
      <span class="topic" id="<%=topic.category%>">
        <% if topic.image.exists? %>
          <%= link_to image_tag(topic.image.url(:thumb)), topic %>
        <% end %>
	<br />
          <%= link_to topic.category, topic %>
          <br  />
          <% if current_admin %> 
            <%= link_to 'Edit', edit_topic_path(topic) %>
            | <%= link_to 'Destroy', topic, :confirm => 'Are you sure?', :method => :delete %>
          <% end %>
      </span>
   </div>
      
  <% end %>

  <div class="clear"></div>
  <br />

  <% if current_admin %>
    <%= link_to 'New Topic', new_topic_path %>
  <% end %>
</div>

<%= content_for(:span, render(:partial => 'offerings/browse'))%>
