
<%= form_for(@profile, :url=>profiles_path, :html => {:multipart => true}) do |profile| %>
  <% if @profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile.errors.count, "error") %> prohibited this offering from being saved:</h2>

      <ul>
      <% @profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%=render :partial => 'form_attributes', :locals => {:profile => profile} %>

  <div class="actions">
    <%= profile.submit %>
  </div>

<% end %>
