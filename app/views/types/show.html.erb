<%title @type.category %>
<p id="notice"><%= notice %></p>

<p>
  <b>Category:</b>
  <%= @type.category %>
</p>

<%= will_paginate @offerings %>
<%= page_entries_info @offerings %>

<% for @offering in @offerings do %>
  <%= render @offering %>
<% end %>

<%= will_paginate @offerings %>
<%= page_entries_info @offerings %>

<% if current_admin %>
  <%= link_to 'Edit', edit_type_path(@type) %> |
<% end %>
<%= link_to 'Back', types_path %>
