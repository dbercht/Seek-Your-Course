<%title @location.state %>
<p id="notice"><%= notice %></p>

<div>
  <% for @offering in @offerings do %>
    <%= render @offering %>
  <% end %>
</div>

<% if current_admin %>
  <%= link_to 'Edit', edit_region_location_path(@location.region, @location) %> |
<% end %>
<%= link_to 'Back', region_locations_path(@region) %>
