# Sample localization file for English. Add more files in this directory for other locales.
# See http://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  hello: "Hello world"
  activerecord:
    attributes:
    errors:
      models:
        offering:
          attributes:
            start_date:
              after: "cannot start before registration begin date"
            end_date:
              after: "cannot be before start_date"
            registration_begins:
              before: "cannot be before offering start date"
              after: "must not be in past"  
